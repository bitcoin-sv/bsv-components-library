plugins {
    id 'java-library'
}

jclGroup = "$jclGroup"
jclVersion = "$jclVersion"
bsvclArtifactName = "bsvcl-bsv"

group = "$rootProject.jclGroup"
version = "$rootProject.jclVersion"

dependencies {

}
